name: run-scraper-once-per-hour
on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:
jobs:
  run_scraper:
    runs-on: ubuntu-latest
    container: mcr.microsoft.com/playwright/python:v1.57.0-noble
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          pip install pipenv
          pipenv install --system --deploy

      - name: Create credentials file
        env:
          GOOGLE_SHEETS_JSON: ${{ secrets.GOOGLE_SHEETS_JSON}}
        run: python -c "import os; f = open('credentials.json', 'w'); f.write(os.environ['GOOGLE_SHEETS_JSON']); f.close()"

      - name: Run scraper
        run: xvfb-run python main.py

      - name: Upload Reports (Screenshots)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-reports
          path: reports/
